framework: jest

postMerge:
  pattern: 
    - "./test/*.test.js"
    - "./bin/nanoid.test.js"
preMerge:
  pattern:
    - "./test/*.test.js"
    - "./bin/nanoid.test.js"
preRun:
  command:
    - npm i -g pnpm
    - pnpm install --shamefully-hoist
postRun:
  command:
    - pnpm --version
version: 1.0
